// Exercise 2: Multi-File Feature
// ASP.NET Core minimal API setup and endpoint registration.
// The AI should wire up the endpoint based on the specification.

var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

// TODO: The AI will register the POST /bookmarks endpoint

app.Run();
